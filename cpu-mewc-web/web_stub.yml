---
- name: Retired public demonstration profile
  hosts: all
  gather_facts: false
  tasks:
    - name: Direct operators to the explicit private service
      ansible.builtin.fail:
        msg: The public web stub is retired. Use web_site.yml with an immutable detector image digest and SSH tunnel.
